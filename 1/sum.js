/**
 * @description Возвращает сумму a и b
 * @example
 * sum(5, 10); // returns 15
 * sum('A', 'B'); // returns 'AB'
 * sum('1n', '2n'); // returns '3n'
 * @template {number|bigint|string} T
 * @param {T} a
 * @param {T} b
 * @returns {T}
 */
function sum(a, b) {
  return a + b
}
